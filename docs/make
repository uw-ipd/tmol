#!/usr/bin/env bash

set -euo pipefail
IFS=$'\n\t'

pushd "$(dirname "${BASH_SOURCE[0]}")/.." >/dev/null

echo "--- Build HTML docs"
export PYTHONPATH="$PWD${PYTHONPATH:+:$PYTHONPATH}"
make -C docs html SPHINXOPTS="${SPHINXOPTS:--W --keep-going}"

popd >/dev/null
