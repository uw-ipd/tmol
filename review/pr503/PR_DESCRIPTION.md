Generalized chemistry currently mixes residue-instance identity, sampler ownership and mutable parameter caches in ways that can change scores or conformers when poses, databases or samplers are reused. This follow-up fixes those contracts, enforces explicit allocation budgets, repairs mirror/grid behavior, and reduces repeated host/device setup and unbounded retention. It targets `dimaio/noncanonicals_through_ligand_pipeline`, including the updates through `0593a93b07d80b0302383163d2d98c78e315ab98`.

This is a review-driven follow-up to #503. Some problems are inherited code assumptions exposed by generalized chemistry; the detailed review distinguishes those from defects introduced by #503. Corrections already made upstream are credited below. Generated force-field files were reviewed through their generators, schema, provenance and executable checks; this is not an independent scientific refit.

## Validation and remaining failures

Current production fixes are **tmol `cdca836b2` / AtomWorks `4af94d0c`**. The immutable `102cda060` broad run finished with **1,667 passed, 15 skipped, 27 failed**; all **18 CPU and 18 CUDA examples pass**. Nineteen failures concerned hydrogen-policy/fixture compatibility and now pass their focused CPU checks; eight inherited fresh-preparation score references remain unresolved. The expanded `6560d12e1` CPU/CUDA run **253088** finished with **1,693 passed, 15 skipped, nine failed** and all **18 CPU + 18 CUDA examples passing**. Eight failures are the noncanonical references; the remaining missing-field test mock is fixed in `88096fafe` and its CPU check passes. The final port-guard/conversion selection has **85 CPU/CUDA passes**, no failures or skips, in **253094**. Current focused selections include **115 semantic/typing passes** and **26 input/topology passes, 20 device skips**, without golden/tolerance changes. These overlap with the broad suite. Both CIF routes now expose a preserve/rebuild hydrogen policy, including D-histidine. Mol2 conversion reuses AtomWorks carboxylate repair only when needed and preserves ring aromaticity correctly. Targeted CUDA scoring/minimization passes complete **145d, 1xvk and 1IZC through both readers**. Original **1AYM declares conflicting MYR partners**; it is now a full-file regression requiring a useful error before graph writes. [Revision-scoped evidence](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/results/chemistry-followup-validation.json). The two CPU/CUDA model-input notebook workflows and local download/install bootstrap also pass; hosted Colab execution is not independently verified.

All **324 corpus trials completed**: 132 local-file trials plus 192 native/AtomWorks trials over the 96 PDBs referenced by AtomWorks IO tests. Local original-input outcomes are tmol **14 pass / 2 partial / 17 fail**, AtomWorks **11 / 2 / 20**; the separate free-metal-excluded diagnostic is **16 / 6 / 11** and **12 / 8 / 13**. Both authored 1j8z variants now score/minimize through AtomWorks; metal-excluded 6w13 moves from parser rejection to partial scoring/minimization because its observed 8OG OP2 is preserved. Every other local status is unchanged from the previous shared checkpoint. Each local mode has four converged trials and four connection-length alerts.

The 96-PDB extension explicitly excludes free metal ions: native **57 / 4 / 35**, AtomWorks **22 / 40 / 34**. Partial means additional residues were excluded by construction, not full-input success. Nine of these 192 trials report convergence; 18 have connection-length alerts. Some differences reflect AtomWorks preserving unresolved chemical identities. Metal-containing components remain present/deferred. New non-metal failures and proposed fixes include crosslinked polymer-port discovery, proximity-inferred false bonds, invalid combined torsion frames, terminal/alias coverage, and incomplete ligand placement. The later 1xvk cap, topology and generic-lookup fixes now pass its complete-fixture CPU regression; these historical corpus counts predate those fixes. [Every input, hash, result and optimizer state](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/results/continued-corpus-validation.json), [continued anchored review and follow-ups](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/CONTINUED_REVIEW.md). Jobs 252914/252915/252916/252917 completed in 21:40/21:38/26:23/22:07 with nonzero exits for retained failures; follow-up 252919 passed. Numerical success does not establish convergence or validate the force field.

AtomWorks' consolidated full IO run has **766 passes, 13 skips and four 1twr failures**; all four reproduce on unchanged dev `59afb1e2`. Added tests were reduced from **151 cases/ten files to 48 cases/six files**, emphasizing complete parsing/protonation/identity workflows. Existing upstream assertions and scientific baselines were not weakened. The original **774 protonation comparisons still match**; three newly added mixed-component comparisons deliberately differ from the old engine because all molecules are now retained.

Earlier shared/pre-sharing checkpoints, full corpus outcomes, environment details and intermediate failures remain in [FOLLOWUP.md](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/FOLLOWUP.md) and its linked validation artifacts. These are historical results, not current-head counts.

The default source is now selected: preserve Frank's hybrid model and generated-geometry Cartesian convention (`K=300` lengths, `K=80` angles). Six CPU whole-pose/block-pair energy and gradient comparisons pass with Frank-convention generated attachment geometry across biotin and both glycan fixtures. This diagnostic retains prototype coverage checks and is not independent stereochemical validation. Completing missing attachment/local terms remains outstanding. The branch supplies explicit parameter ownership, persistence, guarded replacement mechanisms and an opt-in capped-MMFF diagnostic generator; it does not silently install that provisional model by default. Fresh-preparation noncanonical score references also require provenance/reconciliation; they have not been refreshed merely to make tests pass. These are reasons to review this as a draft.

## Every identified problem and its disposition

The 119 numbered items below correspond to the anchored line comments in [the detailed review](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/REVIEW.md). Findings 92–100 are anchored in [the complex-input review](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/CORPUS_FINDINGS.md). Findings 101–108 are in [the protonation audit](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/PROTONATION_AUDIT.md). Findings 109–119 are in [the continued review](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/CONTINUED_REVIEW.md). Each row states the implemented change or the remaining limitation. Historical intermediate failures and superseding fixes are retained in [the follow-up validation record](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/FOLLOWUP.md).

<details>
<summary>Problem → fix/status for all 119 review findings</summary>

| # | Problem | Fix / status |
|---|---|---|
| 1 | missing module prevents test collection | **Fixed.** Supply the missing cyclic-search implementation and verify clean import and cyclic construction. |
| 2 | mixed D/L disulfide energy depends on residue ordering | **Fixed; scientific scope limited.** Use the Rosetta mixed-chirality disulfide distribution with shared derivatives; check LL/DD/LD/DL, permutation and reflection. |
| 3 | group anchors collide across poses | **Fixed.** Key group anchors by pose and block in both producers and consumers. |
| 4 | anchor chi numbering removes child linkage samples | **Fixed.** Separate the anchor-library cardinality multiplier from independently numbered child chi samples. |
| 5 | retained chi can be assigned to the wrong attached residue | **Fixed.** Carry original sample indices through budget selection instead of reconstructing ownership from chi names. |
| 6 | unsafe native pair enumeration before count validation | **Fixed.** Validate declared joint-member counts before native pair enumeration and remove per-block scalar reads. |
| 7 | zero-rotamer trailing blocks index beyond the remap | **Fixed.** Use an exclusive-endpoint prefix map and preserve negative sentinels for empty trailing blocks. |
| 8 | task sampling budget is currently a no-op | **Fixed for explicit per-residue limits.** Propagate task limits through conversion using private sampler views; check actual library, group and aggregate counts before final row allocation. Default and whole-task/pair-memory policies remain open. |
| 9 | group-tree cache aliases unrelated type tables | **Fixed.** Own group-tree caches on their PackedBlockTypes instead of reusing local indices across databases. |
| 10 | CIF completion merges insertion-coded residues | **Fixed.** Use contiguous residue boundaries, including insertion codes, for completion, representatives and cross-residue detection. |
| 11 | canonical conjugation preparation merges chains | **Fixed.** Select one canonical attachment residue instance without merging equally numbered residues across chains. |
| 12 | hydrogen naming breaks template coverage | **Fixed.** Identify hydrogens by element so digit-prefixed hydrogen names do not invalidate heavy-atom templates. |
| 13 | content hash concatenation is ambiguous | **Fixed and optimized.** Frame hashed values by type and length, preserve tensor metadata boundaries and hash buffer views without a full bytes copy. |
| 14 | sampling caches ignore the new budget configuration | **Fixed.** Include sampler configuration and chemistry in annotation validity; retain only the latest tables and isolate task overrides. |
| 15 | repeated allocation in the group conformer loop | **Optimized.** Hoist invariant group-tree tensors and batch forward/inverse kinematics in bounded chunks; retain scalar-reference parity tests. |
| 16 | quadratic CIF array assembly | **Optimized.** Concatenate CIF pieces once, assemble bonds once, cache component templates per read and lazily select representatives. |
| 17 | make unsupported cyclic groups explicit | **Partially addressed by 35.** Preserve cyclic and external constraints by restricting independent axes; alternative ring puckers and a general closure sampler remain outside the implemented contract. |
| 18 | share the cartbonded improper enumerator | **Simplified.** Share connection-centred cartbonded improper enumeration and lookup across all four native score/gradient paths. |
| 19 | remove duplicated atom-removal work | **Simplified.** Remove duplicated surviving-torsion and chi filtering after atom removal. |
| 20 | bound profile cache lifetime | **Fixed by 29.** Replace unbounded integer-identity profile caches with the shared bounded weak-owner cache. |
| 21 | group packing tests silently discard the prepared database | **Fixed.** Make group-packing tests and examples use the returned preparation context's parameter_database. |
| 22 | prepared terminal caps cannot be selected into a pose | **Fixed.** Align terminal-cap representation and candidate classification; validate ACE/NH2/NME construction, geometry, scoring, gradients and packing. |
| 23 | reference score assertions do not reproduce | **Open validation/provenance issue.** Preserve failing score goldens; separate fresh-preparation discrepancies from pinned-input numerical replay instead of silently refreshing references. |
| 24 | the D repacking test checks labels, not geometry | **Improved validation.** Measure signed stereocentre volumes in offered and packed D conformers and verify mirrored-library use; full-atom mirror matching is added in 73–76. |
| 25 | fold-tree tests contradict branch-point splitting | **Resolved upstream; merged.** Retain Frank's corrected fold-tree edge expectations at conjugation branch points. |
| 26 | conjugation selection excludes every ligand fragment | **Fixed.** Select explicit fragment candidates and let the exact original-instance cut-bond mapping own restoration. |
| 27 | HYP rotamer count disagrees with the stated sampling model | **Resolved upstream and independently checked.** Use 18 HYP states and separately verify two library states times nine hydroxyl angles; preserve explicit-budget checks. |
| 28 | terminal proton sampling rotates an entire generated nucleotide | **Fixed.** Root generated nucleotide jump trees on the sugar side so terminal proton sampling cannot rotate the heavy structure. |
| 29 | polymer profile caches can reuse a dead database's identity | **Fixed; bounded retention.** Share bounded weak-identity caches across alpha, nucleotide and rotamer-reference profiles, with expiry, eviction and simultaneous-miss checks. |
| 30 | linkage sampling breaks internal sugar-ring bonds | **Fixed.** Use actual connection-spanning axes for sugar-ring attachments and resolve central atoms in group-wide numbering. |
| 31 | a departing hydrogen's icoor is not necessarily a bonded torsion path | **Fixed.** Derive attachment torsion frames from bonded heavy-atom paths and verify requested Cartesian dihedrals. |
| 32 | missing coordinates must not authorize a bond-order rewrite | **Fixed and shared through AtomWorks #349.** Require finite, nondegenerate geometry before carboxyl bond-order correction; tmol imports the shared implementation. |
| 33 | capped polymer residues disappear from conjugated groups | **Fixed.** Recognize polymer anchors from declared polymer properties after both terminal ports are patched away. |
| 34 | generic conjugated ligands have unsampled internal heavy torsions | **Fixed.** Generate existing generic non-ring heavy-chi samples for movable internal branches of conjugated ligands. |
| 35 | a spanning tree does not enforce cyclic or external constraints | **Fixed for preserving current constraints.** Retain every internal/external edge; restrict axes with bridge/subtree checks, project constrained library coordinates and deduplicate; keep the current conformer when nothing can move. |
| 36 | independent samplers remain enabled on secondary polymer members | **Fixed.** Disable independent samplers on every group-owned member, retaining the intentional library-free primary-anchor exception; 51 rejects later conflicting ownership. |
| 37 | group sampling ignores subsequent task masks | **Fixed within supported chemical types.** Apply sampler/task/allowed-type masks before enumeration and use fixed members as geometric constraints; emit joint rows only for active members. |
| 38 | conjugation patches omit connection bond and angle energies | **Backend implemented; Frank model selected, default implementation pending.** Use Frank's hybrid scoring and generated-geometry Cartesian convention for missing attachment records. Keep the tested capped-MMFF harmonic generator diagnostic-only; it is not the selected default. |
| 39 | protonation cache grows with every pH and exposes mutable rules | **Fixed and shared through AtomWorks #349.** Use an eight-provider compiled-rule LRU without retaining per-pH queries; return owned public rule objects and pass tmol's exact scientific inventory. |
| 40 | injected cartbonded parameters keep the previous cache key | **Fixed.** Recompute cartbonded content identity on parameter injection so reused poses score the new fit. |
| 41 | conjugate params export omits the canonical partner | **Fixed.** Persist canonical-partner patches/charges, explicit connection records and provenance; reload them even when base residues already exist, preserving numeric geometry and bond order. |
| 42 | every exported atom record leaves a class in a global registry | **Fixed and optimized.** Register one reusable YAML flow-mapping class instead of retaining one class per exported atom/parameter row. |
| 43 | acylation uses only the hydrogen count from the derived chemistry | **Replacement mechanism implemented; Frank model selected, corrections pending.** Preserve Frank's typing/charge and Rosetta/generic ownership conventions when completing local attachment chemistry. The private MMFF-delta prototype is diagnostic-only; coupled default corrections remain incomplete. |
| 44 | polymer capping discards source atom annotations | **Fixed with an explicit tradeoff.** Preserve input atom annotations and long names during capping; add a cheaper topology-only path for model generation. Coordinate-producing capping pays a small metadata-preservation cost. |
| 45 | electrostatic annotations retain the first database's parameters | **Fixed.** Key electrostatic annotations by source/typing configuration, snapshot rendered tensors and support complete multi-patch variant charge/count-pair rows. |
| 46 | unresolved attachment coordinates become NaN construction parameters | **Fixed for invalid observations.** Ignore unresolved/coincident attachment measurements, retain earlier valid observations and use full residue identity; process geometry in chunks. Distinct valid context-specific observations still require a broader policy. |
| 47 | equal component names do not identify a fragment cut bond | **Fixed after upstream update.** Preserve explicitly declared links between repeated fragment types; remove only exact expansion-owned cuts rather than every equal-base-name link. |
| 48 | the conjugated-only fallback retains just the first candidate | **Fixed after upstream update.** Retain all valid fragment states independently of their input ordering using explicit fragment identity. |
| 49 | connection impropers are limited to fragments of one source ligand | **Fixed mechanism; automatic chemistry remains 43.** Enumerate generic connection impropers according to the centre's scoring ownership; add explicit per-atom generic lookup references without transferring canonical proper-torsion ownership. |
| 50 | missing-leaf construction recognizes only polymer connection names | **Fixed.** Resolve missing-atom construction references through any declared connection name, preserving absent polymer-end fallback. |
| 51 | chemical connectivity does not establish conformer correspondence | **Fixed and shared.** Carry producer-declared joint-conformer groups through merge, reject overlapping/additional owners before allocation, and share one validated mask across energy enumeration and collapse. |
| 52 | OptH selects an attachment torsion as an amide flip | **Fixed.** Use actual ASN/GLN/HIS flip axes and disable flips moving covalently attached atoms; share eligibility with fallback/root selection. |
| 53 | generic bonded annotations retain another database's parameters | **Fixed and optimized.** Snapshot generic bonded annotations by database and element mapping; share database-wide inter-block tables through a bounded weak cache. |
| 54 | Cartbonded ownership outlives the configuration that defined it | **Fixed.** Include Rosetta/generic ownership in cartbonded annotation snapshots; share identical masks across fits and preserve older rendered modules. |
| 55 | bonded annotation dictionaries retain every fitted parameter set | **Bounded retention.** Keep two recent cartbonded configurations per block/packed set; rebuild evicted configurations while existing modules retain valid snapshots. |
| 56 | common atom setup synchronizes once per atom | **Optimized.** Use host atom-type indices and hydrogen flags for common atom setup, removing thousands of device-scalar reads and repeated name lookup. |
| 57 | peptide junction substitutions do not validate chemical roles | **Fixed after latest upstream update.** Validate peptide junction element/bond roles on the final reconstructed graph; reject phosphate-as-nitrogen substitution and retain generated hydrogen angle rows. |
| 58 | a canonical connection name can hide a noncanonical junction frame | **Partially addressed.** Map a changed complete local junction frame even when its connection atom is still named C/N. Simultaneously renamed partner frames remain a parameter-coverage question. |
| 59 | cap completion adds a second coordinate builder and dense tables | **Simplified after latest upstream update.** Build cap hydrogens through the existing native connection-ancestor representation; remove the extra Python coordinate pass and four dense packed fields while matching upstream hydrogen conventions. |
| 60 | nonbonded indices and pair exclusions retain a different configuration | **Fixed.** Refresh nonbonded atom indices and pair exclusions for catalog/ownership changes using weak identities and rendered snapshots. |
| 61 | hydrogen-bond and LK-ball scorers read mutable shared annotations | **Fixed.** Snapshot hydrogen-bond and LK-ball annotation records so reusing shared block types cannot change an older scorer. |
| 62 | hydrogen-bond resolver caches are unbounded and cannot verify their owners | **Bounded retention.** Use a multi-owner weak-identity LRU for both hydrogen-bond resolver caches, capped at 32 entries with normalized device identities. |
| 63 | residue annotation repeats catalog lookups and full parameter transfers | **Optimized.** Prepare donor/acceptor host lookup catalogs once and transfer LK-ball host parameters once per term instead of per residue. |
| 64 | native rotamer counts can wrap before allocation checks | **Fixed allocation boundary.** Check native Dunbrack count products, prefix sums and library-size narrowing before allocation, reusing existing buffers and synchronization points. |
| 65 | Validate nucleotide counts before narrowing or allocation | **Fixed allocation boundary.** Retain nucleotide counts in int64, validate products/individual counts/totals and reject invalid counts before narrowing or allocating rows. |
| 66 | Key Dunbrack sampler annotations by their parameter source | **Fixed and optimized.** Key Dunbrack sampler annotations by weak resolver identity; gather small metadata on the host once and simplify common probability selection and sampler equality. |
| 67 | Include polymer types that sample their own heavy chi | **Fixed.** Honor the sampler's buildability predicate for polymers with explicit heavy chi and no library; preserve gapped chi slots and derive roots from sampled axes. |
| 68 | Refresh mainchain-copy fingerprints when sampler ownership changes | **Fixed and optimized with tradeoffs.** Key mainchain fingerprints by actual sampler roots and chemical owner; preserve unions of nonnested source regions, refresh stale fingerprints and correct the packed cache guard. |
| 69 | Bound the Dunbrack resolver cache and release private databases | **Bounded retention.** Replace the global Dunbrack resolver memoizer with a four-entry weak-owner LRU; release private derived tables when callers release their database/resolver. |
| 70 | Keep Dunbrack scoring annotations tied to their resolver | **Fixed and optimized.** Key Dunbrack scoring annotations by resolver, retain rendered snapshots and assemble host annotation tables before one transfer per field. |
| 71 | Keep DOF-copy indexing on device and repair its source oracle | **Simplified and optimized.** Gather DOF-copy indices on device through a shared atom map, reuse owned buffers, and repair the self-comparing source oracle and fixture considered-type assumptions. |
| 72 | Keep chi assignment and ring-offset lookup on the tensor device | **Simplified and optimized.** Keep chi assignment and static ring-offset lookup on device; use sparse selection and support noncontiguous chi columns. |
| 73 | Validate reflected conformer sets, terminal defaults and probability ordering | **Fixed for the validated mirror contract.** Correct reflected ordering cells and chirality-aware missing-angle defaults; combine with 75–76 for one-to-one full-atom conformers, score matrices and a controlled exhaustive packing check. |
| 74 | Wrap the positive periodic endpoint before sorted-table indexing | **Fixed.** Share periodic lookup with an exclusive upper endpoint and a guard against division rounding to an out-of-range bin. |
| 75 | Reflect the chi-mean branch before spline fitting | **Fixed.** Persist explicit library reflection metadata, reflect chi-mean unwrapping before fitting and score periodic chi-minus-mean differences. |
| 76 | Rebuild both glycine alpha hydrogens and define the symmetry option completely | **Fixed; symmetry remains opt-in.** Rebuild both glycine alpha hydrogens; average their equivalent ideal/bonded parameters only in the explicit symmetric-glycine database. |
| 77 | Generate missing D libraries from requested mappings, not a name-prefix guard | **Fixed and optimized.** Generate only missing requested D mappings/libraries, preserve explicit targets and existing objects, reject ambiguous names and avoid work for no-op requests. |
| 78 | Support rotamer-only and empty library databases through the native boundary | **Fixed; minimal databases supported.** Use rank/stride-correct genuinely empty tensors and offsets for absent statistical-library families without dummy tables or dihedrals. |
| 79 | Register reflected grids at their actual coordinates | **Fixed.** Carry actual reflected grid origins and declared semirotameric spacing, preserving sampling-cell metadata and serialization without resampling. |
| 80 | Read each table's metadata and respect padded spline strides | **Fixed.** Address the selected metadata vector correctly, interpolate using actual padded tensor strides and fit into owned contiguous coefficients. |
| 81 | Correct periodic fitting for small spline grids | **Fixed.** Correct short-period spline initialization so small grids interpolate their supplied samples; preserve the large-grid branch. |
| 82 | Reuse periodic indices inside spline interpolation | **Optimized with measured device tradeoffs.** Reuse periodic indices in spline interpolation, selecting bounded caches for CPU/2D CUDA and base indices for larger CUDA stencils; preserve accumulation order. |
| 83 | Register each sampler object once and combine its enabled regions | **Fixed and simplified.** Own sampler lists, register each object once and union enabled masks; preserve distinct equal samplers and prevent palette mutation. |
| 84 | Reject unresolved or ambiguous library mappings before fitting | **Fixed and optimized.** Validate unique library names, residue mappings and declared targets before fitting; build one metadata mapping and permit intentionally omitted lookup rows. |
| 85 | Share current-conformation copying and avoid padded index work | **Simplified and optimized.** Share IncludeCurrent/Fallback copying, enumerate only copied atoms, eliminate unrelated-database padding costs and remove global CUDA barriers. |
| 86 | Define guarded replacement semantics for reusable parameter bundles | **Implemented explicit guarded semantics.** Persist typed baseline guards for exact residue replacements; reject stale targets and install public/private bundles with one whole-database cartbonded rebuild. |
| 87 | Coalesce repeated batch definitions and reject contradictory sources | **Fixed and optimized.** Coalesce identical residue/charge/bonded definitions, reject contradictions, stream shared charge merges, load repeated paths once and avoid mutating inputs. |
| 88 | Preserve declared elements for custom atom types through parameter export | **Fixed and optimized.** Persist declared custom atom-type elements in version-5 bundles, collect types from residues/patch additions/modifications/replacements together and reject incompatible element declarations. |
| 89 | Reject missing parameters for used atom types before scoring | **Fixed and optimized setup.** Validate used real-atom types and finite LJ/solvation fields before scoring; preserve unused incomplete rows and virtual zeros, and reuse validated indices/heavy lists during packed setup. |
| 90 | Require explicit charges instead of silently zeroing an unknown residue | **Fixed with small validation cost.** Require applicable finite charges instead of silently zeroing missing residue tables; store the existing three zero HOH charges explicitly without introducing a water model. |
| 91 | Build complete hydrogen-bond pair tables in bulk | **Fixed and optimized.** Validate complete declared donor/acceptor pair coverage, references and finite selected data; preserve last-definition precedence and empty-family support while assembling tables in bulk. |
| 92 | incomplete-residue filtering severs a retained covalent partner | **Fixed.** Reject removed/retained sidechain, disulfide, cyclic and cross-chain covalent boundaries; preserve ordinary forward peptide gaps. The Schiff-base fixture now errors instead of scoring a severed component. |
| 93 | unmapped observed heavy atoms disappear at canonical conversion | **Fixed with a bounded reader guard.** Reject unmapped non-H/D atoms and check source heavy-atom names after AtomWorks completion. Native modified-1a8o author CG is preserved; label XYZ cannot silently disappear. The reader guard is an inventory check, not full atom/bond provenance. |
| 94 | aromatic acyl cap ordering stops before the retained scaffold is complete | **Fixed and simplified.** Traverse all retained non-sidechain heavy branches and use set membership for placed atoms. The 6q9t 4SO cap/partner regression has complete finite heavy-atom geometry. |
| 95 | LLP selects a nucleic-acid terminal patch by atom-name overlap | **Fixed.** Require the actual variant suffix and patch applicability to the base type. LLP terminal OXT receives the correct charge; native whole-file 7MKV scoring and minimization pass. |
| 96 | sidechain conjugation ports are treated as backbone endpoints | **Fixed for identified conjugations.** Exclude conjugation endpoints from chemistry-derived backbone profiling and use the same filtered ports in polymer preparation. BCX N/C remains a peptide backbone while SG remains connected as a disulfide. |
| 97 | missing nonleaf ancestors prevent valid leaf-coordinate fallback | **Fixed in shared construction.** Pass the complete missing-atom mask and compose a valid fixed-frame backup without replacing a sampled torsion. Conditional-generation N/CA/C inputs with unresolved CB now build finite HA coordinates, scores and derivatives. |
| 98 | CIF preparation rereads an already parsed component category | **Simplified and integrated.** Reuse chemical-type annotations from the parsed block. Use the pinned AtomWorks core dependency and a public reader option shared by examples/profiling. Current ParseConfig replaces version dispatch; native authored-CIF category parsing also delegates to AtomWorks. |
| 99 | AtomWorks removes every possible leaving branch for one polymer connection | **Fixed in AtomWorks #349.** Retain distinct chemically equivalent branches and displace only the declared bond-order budget; preserve observed 8OG OP2. Ambiguous observed alternatives remain explicit errors, and AF3 OXT normalization/provenance policy remains open. |
| 100 | Explicit H without H bonds disables implicit-H inference | **Fixed with explicit reader policy.** Both bonded CIF routes default to rebuilding H while retaining finite histidine evidence, including DHI. Expose preservation explicitly. Complete 1IZC passes CUDA scoring/minimization through both routes. |

| 101 | Scientific rule inventories differ | **Fixed with explicit scientific policy.** Use one AtomWorks engine with tmol rule ordering/pKa data, pH/precision and variant budget; 774 mapped-state comparisons match the pre-sharing tmol engine. |
| 102 | Histidine postprocessing loses bonds or fails to enforce state | **Fixed in AtomWorks #349.** Resolve finite HIS/DHIS evidence before H removal, enforce ring H counts and charges before AddHs, and preserve cross-residue bonds and repeated/assembly identity. |
| 103 | Neutralization reaction order breaks atom correspondence | **Fixed in AtomWorks #349.** Restore reaction-source atom properties, input order and conformer coordinates before returning sanitized molecular variants. |
| 104 | Molecular fallback loses geometry and metadata | **Fixed in AtomWorks #349.** Retain molecular fallback states instead of reconstructing them from SMILES; exercise invalid-final-state fallback identity. |
| 105 | Reader strips histidine tautomer evidence | **Fixed in tmol adapter.** Preserve finite input histidine ring H through AtomWorks parsing so native tautomer selection receives the observed evidence. |
| 106 | Template rematching duplicates graph work | **Simplified in AtomWorks #349.** Remove redundant template graph rematching and consume the identity-preserving protonated molecule directly. |
| 107 | RDKit H/coordinate and ownership contracts differ | **Shared and optimized.** Use AtomWorks RDKit conversion with explicit H/coordinate controls, owned retained annotations and bulk conformer positions; remove tmol bond-map/restoration duplication. |
| 108 | Authored CIF chemistry has two parsers | **Shared with explicit authority.** Use AtomWorks authored-CIF chemistry without CCD supplementation; preserve tmol template semantics and remove the second component/bond-category parser. |
| 109 | Stale source carbon charge permits pentavalent ACE | **Fixed in AtomWorks #349.** Limit carbon to four bonds during leaving-H resolution, then correct charge; full authored 1j8z workflows pass. |
| 110 | Neutralizing one component discards other molecules | **Fixed in AtomWorks #349.** Neutralize components independently and combine all products before restoring source identity; avoid copying the common connected case. |
| 111 | Save/load control flags defeat parse caching | **Fixed in AtomWorks #349.** Exclude storage controls from chemical cache identity; the unchanged integrated 4NDZ cache workflow passes. |
| 112 | Inter-residue detection allocates large string arrays | **Optimized in AtomWorks #349.** Use patched contiguous-residue integer indices and reuse extracted bonds, preserving assembly identity. |
| 113 | Internal alpha-like motif renames a cap's reference frame | **Fixed in tmol.** Restrict alpha renaming to alpha profiles and retain cap frame names; complete 1xvk now prepares, scores and minimizes with the subsequent topology/lookup fixes. |
| 114 | Nonstandard/crosslinked polymers lose backbone-port identity | **Targeted fix validated on CPU/CUDA; broader contexts remain.** Resolve chemically supported ports, preserve all explicit links and derive termini from occupancy. Complete 1xvk passes both-reader scoring/minimization; original 1AYM has the separate source conflict in 119. |
| 115 | Proximity invents conjugations despite a supplied bond graph | **Fixed.** Remove proximity-inferred conjugations and consume the supplied bond graph. Recognize terminal nucleotide backbones before peptide caps; complete 145d retains 24 DNA blocks and 20 phosphodiester links and passes CPU scoring/minimization. |
| 116 | Combined patches reference removed atoms | **Guard implemented; alternate frames remain open.** Validate new and retained torsion support before mutation. The original 1AYM missing-H failure is gone; both routes expose its conflicting source links (119). |
| 117 | inter-block generic torsions depend on residue order | **Generic torsion lookup fixed; CUDA validation pending.** Share reversed matches and multiplicity/source-order rank with the CPU lookup; use the actual central bond bin for 3+1 paths. Fix the 70.621246 energy change under 1xvk residue reversal; cross-block ring metadata remains a separate limitation. |
| 118 | Mol2 annotations restore invalid non-ring aromaticity and lose the carboxylate carbonyl | **Fixed and optimized.** Require ring membership for aromatic flags; reuse AtomWorks bond-order repair only for unresolved Tripos carboxylates. Integrated acetate/biphenyl failures now pass through chemistry, preparation, scoring and gradients; 115 semantic/typing cases pass. |
| 119 | Conflicting declared partners overwrite one connection port | **Fixed; original input explicitly rejected.** Validate occupancy before device writes and identify both partners. The complete 1AYM fixture declares GLY N–MYR C1 and GLY CA–MYR C1; both-reader regressions require an explicit conflict error and unchanged source graph. |
</details>

## Efficiency and simplification

These measurements compare each change with its recorded parent implementation, not a single untouched-upstream versus final-branch run. Setup stages overlap and their speedups must not be added. CPU and H200 CUDA environments, exact input/source hashes, repeated measurements, equality checks and memory definitions are in the linked manifests and `FOLLOWUP.md`. The current H200 container executes the measured sm75/PTX75 kernels through driver JIT; these are not native-sm90 compilation claims.

| Area | Implemented efficiency change and measured scope |
|---|---|
| Shared chemistry implementation | Replace over 1,500 lines of duplicated tmol production code with tested AtomWorks chemistry, retaining explicit force-field policy. No claim that generic pH titration equals input-selected histidine or force-field partial charges. |
| Shared RDKit conversion | Direct source columns and bulk coordinates: 20/200 atoms about 1.9×/1.8× faster; 3,000 atoms plus unused metadata 58.920 → 14.116 ms. Python-traced peak 49.261 → 0.176 MB; native allocations excluded. |
| Shared parser boundary | Reuse the already parsed component category; share one AtomWorks adapter across public construction, examples and profiling. Cap ordering uses set membership and traverses the complete retained scaffold. No isolated application timing is claimed. |
| CIF completion | One concatenation/bond assembly plus per-read template reuse; 2,000-residue insertion benchmark 266.93 → 67.98 ms (3.93×). This excludes complete parsing/preparation. |
| Content hashing | Type/length framing plus buffer-view tensor hashing avoids ambiguous serialization and a full Python bytes copy. |
| NA annotation | Exclude unrelated protein/glycan chi records; tested glycan proton-sampling tensor storage falls 80%. Timing was a single-run diagnostic. |
| Extra anchor-library pass | Mask to actual group anchors: biotin 1,781 → 60 rows, O-glycan 3,028 → 3, N-glycan 5,849 → 66; CPU stage 1.46–1.80× faster, GPU latency essentially unchanged. |
| Explicit group budgets | In the tested N-glycan configuration, 595 → 67 conformers and chi arrays 11,900 → 804 bytes; enumeration CPU 1.571 → 1.144 ms, GPU 3.224 → 2.294 ms. Required states raise if they cannot fit. |
| Group kinematics | Hoist invariant tensors and batch bounded transforms; full rotamer construction improves 6–11% CPU / 21–33% CUDA, with increased temporary workspace. |
| Joint-conformer ownership | Produce one validated joint mask per rotamer set and share it across scoring terms and collapse, avoiding repeated chemical-graph inference. |
| Rotamer merge | Replace global sorting/unique scans with prefix destinations. At 1,548,739 rows: CPU 100.564 → 14.524 ms; GPU 1.059 → 0.654 ms; extra GPU allocated peak 116,422,144 → 46,385,152 bytes. Tiny merges are essentially unchanged. |
| Polymer/reference caches | Shared weak-owner LRU; 100-database churn leaves zero profile entries instead of 300. Bounds apply to retained cache entries, not arbitrary object sizes. |
| Protonation rules | One private compiled rule inventory; tmol 500-pH retained traced allocation 16.10 MB → 0.065 MB. Warm molecule latency slightly increases, 0.509 → 0.527 ms. |
| YAML export | One flow-marker class prevents global representer growth. Twenty exports retain about 1.1 KB after collection instead of 10–54 MB; median writing improves 1.06–1.08×. |
| Polymer capping | Topology-only path is 2.46–2.64× faster. Coordinate-producing capping is 7–13% slower because source annotations now survive; about 20–33 μs extra per call in that measurement. |
| Attachment measurements | Chunk bond/coordinate temporaries and discard invalid measurements without copying or filling the input's missing coordinates. |
| Explicit attachment backend | Share sparse connection lookup and one native evaluator across whole-pose, block-pair and rotamer paths; avoid a dense all-connection parameter expansion. Its opt-in generator and backend have separate profiles, not a default-workflow speedup claim. |
| Generic bonded setup | Share database-wide inter-block parameter tables through a bounded weak cache; preserve per-term immutable snapshots. |
| Electrostatic setup | Reuse unchanged representative-distance geometry across charge updates; retain only current owner annotations while rendered modules own their parameters. |
| Cartbonded cache | Retain two fits instead of every fit. Six-fit packed cache tensor storage 9,379,104 → 3,170,528 bytes; block NumPy arrays 5,974,848 → 1,991,616 bytes. Evicted fits must be rebuilt. |
| Common atom setup | Eliminate 4,968 native scalar reads over 230 types / 4,738 atoms. CPU 19.863 → 7.646 ms; CUDA 72.613 → 10.255 ms; annotation values exact. |
| HBond/LK-ball annotation | Gather from one host catalog and transfer LK-ball host data once. Standalone setup: HBond CPU 262.27 → 117.76 ms / CUDA 388.17 → 151.62 ms; LK-ball CPU 331.99 → 178.49 ms / CUDA 508.93 → 223.46 ms. Small persistent host arrays increase; device storage is unchanged. These stages share work. |
| HBond resolver retention | Multi-owner 32-entry caches: 128-fit cached tensor storage 16,518,144 → 4,129,536 bytes, then zero when sources expire. Caller-owned outputs remain valid. |
| HBond table assembly | Bulk validated host assembly replaces tiny tensor assignments: cold CPU resolver 64.37 → 0.796 ms, compact including resolver 62.79 → 0.990 ms; CUDA 78.15 → 1.048 ms and 78.75 → 1.399 ms. Default logical table storage remains 129,048 bytes; traced peaks fall about 1.8 KB on CPU. |
| Dunbrack sampler setup | Resolve small metadata once on the host: CPU 64.738 → 9.425 ms / CUDA 104.047 → 10.741 ms; annotation tensor/array bytes 212,846 → 211,006. Resolver fitting and sampling excluded. |
| Mainchain fingerprints | Reuse plans with valid sampler-root ownership: default three-sampler cold setup 1.45× CPU / 1.40× CUDA and 4.1% fewer packed bytes. The two-sampler case is 4.8–7.2% slower cold and uses 2.8% more packed bytes; warm reuse improves. |
| Dunbrack resolver retention | Four-entry weak-owner cache releases a measured 67,494,320-byte private derived table set when callers release its source/resolver. Active outputs are retained correctly. |
| Dunbrack scoring setup | Host table assembly: CPU 55.487 → 3.117 ms / CUDA 110.069 → 3.767 ms across 230 types; packed storage unchanged. Validated warm hits rise from about 26 to 44–45 μs. |
| DOF-copy plan | Device gathers replace CPU round trips and remove 123 production lines. A 36,000-conformer case: CUDA 2.610 → 0.324 ms, extra allocated peak 22,110,208 → 14,440,448 bytes; the shared lookup table is accounted for separately. |
| Chi assignment | Sparse device selection and one ring-offset table remove 19 production lines. At 3,000 conformers: CUDA 0.407 → 0.141 ms, extra allocated peak 358,400 → 320,512 bytes. |
| IncludeCurrent/Fallback | Share copying and enumerate actual copied atoms, removing 76 net production lines and global CUDA barriers. In a 12,000-conformer case with an unrelated 1,024-atom type: GPU planner 0.498 → 0.185 ms and extra peak 118,974,464 → 6,471,680 bytes. Real 16-pose DOF fill: CPU 1.060 → 0.671 ms / GPU 0.381 → 0.220 ms; whole construction changes only about 1–4%. |
| Selective mirrored libraries | DARG-only generation 17.28 → 1.65 ms and owned tensors 30,927,608 → 4,279,200 bytes; DSER-only 17.29 → 0.089 ms and 77,784 bytes. Full default requests remain essentially unchanged; no-op requests allocate no mirrored tensors. |
| Minimal statistical libraries | Genuinely empty/absent families need no dummy tables. LEU-only/PHE-only/no-library derived storage is 374,345 / 1,308,573 / 0 bytes; these deliberately select different model coverage and do not prune a full model automatically. |
| Lookup validation | One mapping replaces repeated lookup construction: default 0.664 → 0.288 ms; 10,040 aliases 8.365 → 1.898 ms. Peak traced memory falls; retained index-engine memory can rise slightly. |
| Spline interpolation | Reuse wrapped indices while preserving accumulation order. In a 64-pose CPU workload, Dunbrack forward/forward+gradient improves about 22%/19%, backbone forward about 17%; GPU Dunbrack forward about 11%, other measured GPU paths essentially unchanged. Managed allocation peaks are unchanged; one driver-compiled float32 Dunbrack pose-forward kernel trades fewer registers for 16 extra bytes of local memory per thread. |
| Cap construction consolidation | Use native connection ancestors instead of a second Python builder; omit four dense fields totaling 24 bytes per padded atom. This is a storage/layout simplification, not a measured application speedup. |
| Parameter bundle installation | Build/hash the final bonded database once; private biotin/N-/O-glycan bundle installation improves about 38–40% with scientifically identical supplied records. Generation and native scoring are excluded. |
| Repeated parameter inputs | Parse a repeated path once and coalesce identical definitions: 100 repeated files 277.772 → 9.750 ms, 100 duplicate definitions → one. Single-file timing is essentially unchanged. |
| Atom-type collection | One shared batch collector covers residues and patches: 1,000-source known-type collection 10.951 → 0.538 ms; repeated custom type 12.095 → 0.568 ms. One-source cost and peak allocation are essentially unchanged. |
| LJ/solvation packed annotation | Reuse validated atom/heavy indices: CPU 7.55 → 2.79 ms / GPU 10.02 → 3.37 ms; traced CPU peak 382,047 → 312,444 bytes. Finite-coverage validation adds about 0.10 ms to resolver construction. |
| Registry and native refactors | Deduplicate sampler objects and union masks without growing columns; share improper enumeration across four paths; remove duplicated atom-removal work. These reduce duplicate work/code without claiming an isolated application speedup. |

Correctness costs are retained deliberately: checked native counts add measurable count-stage overhead; finite charge validation adds about 2.6 μs for alanine and 0.62 ms for the full 230-type catalog. CPU LJ/LK whole-pose accumulation now uses double precision and casts once, reducing the tested discrepancy against precise pair summation about 33-fold, at roughly 3–5% CPU latency cost and 24 bytes of scratch per pose. CUDA accumulation is unchanged. These are not performance wins disguised as such.

Additional validation repairs include restoring native HBond point-score bindings and removing two inherited expected-failure markers while retaining the original point energies and finite-difference tolerances; repairing the DOF-copy test that compared its expected source array with itself; and enabling the previously skipped large CPU glycan packing/energy checks. The full-source benchmark scripts and stage manifests are committed under `review/pr503/`.

## AtomWorks reuse and companion work

The tmol side consumes a completed AtomArray and its declared chemistry annotations, so AtomWorks parsing can supply unresolved atoms and connectivity without a second structure parse. The local corpus below exercises both this route and tmol's reader on the same normalized files. Tmol still owns parameter generation and native numerical scoring.

The current companion is [AtomWorks #349](https://github.com/baker-laboratory/atomworks-dev/pull/349), based on latest `dev` `59afb1e2` and pinned at `4af94d0c`. This tmol branch makes AtomWorks a **core dependency** and removes its duplicate Dimorphite engine, carboxylate/radical-O repairs, bond-map/restoration code, and authored CIF category parser. It preserves tmol's scientific rule data and force-field-specific normalization. The compatibility extra `tmol[atomworks]` remains empty. Native tensor/GPU histidine resolution stays in tmol and is directly compared against the shared selector.

**Dependency publication requirement:** the draft uses an immutable authenticated Git/SSH dependency on the AtomWorks companion commit. It requires access to `baker-laboratory/atomworks-dev` and is not yet a public-release dependency; replace it with the released version containing #349 before public package publication. Core dependency resolution via `uv pip compile` succeeds and selects Biotite 1.6.0; this is not a clean install/test of every extra. The earlier AtomWorks 2.2.1 compatibility matrix applies to pre-sharing commit `01159dd7a`, not this new required API. The native CIF reader remains the default while author/label, leaving-group and full atom-provenance policy is settled.

The protonation audit found rule/data differences, incomplete older local HIS postprocessing, atom reordering/property loss through reactions, metadata loss through fallback SMILES, and loss of input HIS H at the tmol adapter. These are addressed under an explicit compatibility policy. **774 original ordered mapped-state comparisons match** the prior tmol engine. The current consolidated inventory has 759 comparisons: three deliberately differ because the old engine discarded four components of the new mixture fixture; the five other AtomWorks-default inventory differences remain scientific-policy differences. **48/48 consolidated additions**, **24/24 integrated cache/mixture/structure checks**, and the **766-pass full IO suite** are recorded above; these are overlapping selections. The legacy vendor self-test still stops at the same inherited secondary-aniline/protective-rule assertion on latest AW dev, pre-sharing tmol and the shared engine. No rule data or expected output was changed to pass it. [Detailed policy](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/PROTONATION_AUDIT.md), [continued comparisons and test consolidation](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/CONTINUED_REVIEW.md).

Against the converter in latest AtomWorks dev, isolated 20-/200-atom inputs improve 0.230 → 0.123 ms and 1.812 → 0.986 ms. A 3,000-atom/32-unused-annotation case improves 58.920 → 14.116 ms and Python-traced peak 49.261 → 0.176 MB. These are nine-repeat warm microbenchmarks with equal output chemistry/coordinates, not whole-workflow speedups or total native-memory measurements. Historical old-companion measurements remain in [ATOMWORKS.md](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/ATOMWORKS.md).

The intended input architecture is **AtomWorks for file parsing/chemical metadata plus direct Torch coordinates through named layouts or explicit atom mappings**. Atom14/atom37/backbone4 are layouts, not separate model APIs. **The OpenFold/RF2 constructors, their ordering/packed-type factories, exports and vendored tables are now removed fully; there are no compatibility wrappers.** This removes another **1,922 production/vendor lines**, beyond the earlier 1,532 net shared-chemistry removal. [The executable tutorial](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/notebooks/example_02_model_inputs.ipynb) shows both model examples as application code; RF2 explicitly selects preserved/rebuilt hydrogens, and both keep supplied-coordinate gradients. The example caches mappings, not final pose topology. A unified prepared-topology API for repeated guidance remains a proposal; PR #380 should become a thin backbone4 adapter plus shared completion tests. See [INPUT_CONTRACT.md](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/INPUT_CONTRACT.md) for the worktree comparison and anchored #380 comments.

A further AtomWorks extraction benchmark at 1k/10k/100k atoms changes 1.349/17.273/177.221 ms to 0.034/0.187/1.680 ms. At 100k atoms, Python-traced peak changes 136.014 MB to 3.781 MB. These nine-repeat synthetic measurements preserve identical bond indices and exclude native allocation; they do not claim equivalent whole-parse speedup. [Benchmark and scope](https://github.com/kierandidi/tmol/blob/review/pr503-chemistry-efficiency/review/pr503/results/residue-bond-benchmark.json).

## General review questions

The questions below preserve the scientific/API decisions raised during review; implemented safeguards and later fixes are listed above. Historical requests about missing code and cache behavior now have corresponding fixes in this branch.

<details>
<summary>All 17 design, provenance and release questions</summary>

1. **Delivery and validation:** Can you add the missing cyclic-search file and enable CI on a fresh checkout? No GitHub checks were reported for the pinned head when reviewed. Which Python/PyTorch/Biotite/RDKit/OpenBabel versions generated the baselines?
2. **Scientific partitioning:** What independent Rosetta/Hahnbeom reference validates the cartbonded correction and generic/Rosetta boundary, beyond regenerated tmol goldens? Can we separate these global score changes from adding new residue classes, so canonical score shifts are auditable?
3. **Mixed chirality:** What should the S–S torsion distribution be for L–D versus D–L? It must be invariant to input residue ordering, and full mirror tests should cover LL↔DD and LD↔DL, including gradients.
4. **Budget semantics:** Is `set_chi_sample_budget()` meant to bound library rotamers, sampled heavy chi, proton combinations, group conformers, total block rotamers, or pairwise memory? What happens when the anchor library alone exceeds the limit, when all child chi freeze, or when required proton samples exceed it? Does a frozen chi retain its input angle or use ideal residue geometry, and is that policy consistent for upstream and downstream axes?
5. **Group constraints:** What is the contract for a partially disabled group, multiple polymer anchors joined by a conjugate, free oligosaccharides, and groups with cycles? Should unsupported topologies fail clearly or remain frozen? Which bond closes a non-tree cycle during sampling/minimization?
6. **Chemical authority:** How should custom residue names with declared bonds/stereochemistry but missing coordinates work under `use_ccd=False`? Currently coordinate completion can still require a CCD component. What explicit input describes ambiguous polymer ends instead of selecting the conventional backbone heuristically?
7. **Sampling versus scoring:** The graph matcher borrows sidechain references for sampling, while scoring ownership is independently inferred. Which tests guarantee that changing a sampling reference cannot silently change the scored potential or leave a rotatable bond unconstrained? How were graph-match thresholds and unknown-base averages chosen?
8. **Lifecycle and reproducibility:** Are samplers intended to be reusable across databases/tasks? Are generated chemistry and cache entries bounded in a long-running service? Can users persist the seed, generated parameters, chosen references, warnings, and exact input authority in a build report?
9. **API and migration:** The PR body says `process_ligands=True`, but the implementation uses `prepare_ligands=True`. Is removing `sample_proton_chi` intentional? Can public docs show the supported CIF, AtomArray, SMILES/mol2, cyclic, and group-packing entry points with executable examples?
10. **Scale and release:** What canonical-protein load/score/pack baseline is acceptable after eagerly adding mirrored tables and invoking group discovery in scoring setup? Can the PR be split along its existing commit sequence into import/preparation, score corrections, and group packing?
11. **Shared AtomWorks chemistry:** Can AtomWorks own CIF completion and chemical annotations, with tmol consuming a completed AtomArray? Before replacing the default reader, which author/label identifiers, insertion codes, alternate locations, unresolved residues and CCD authority must be preserved? Can the copied Dimorphite and pre-protonation rules use one versioned shared API, while tmol keeps parameter generation and numerical scoring?

12. **Shared rule provenance:** Tmol adds an enamine SMARTS rule with pKa 1 ± 1 that AtomWorks does not contain. What evidence supports its scope and values, and should it be a shared default or an explicit preparation profile? Direct replacement currently changes charge states for an enamine and a vinylogous amide at pH 2 and 7.4. Which complete rule inventory and model version should exported parameters record?

13. **Attachment charge policy:** Should local charge changes preserve the curated residue baseline and add a connected-versus-disconnected MMFF correction, or replace the whole capped group's charges? The former preserves remote backbone parameters but is a model choice requiring validation. Complete capped biotin changes formal charge by −1, with heavy-atom-plus-hydrogen deltas on both LYS and BTN, including LYS CE. Applying only a hydrogen-count patch cannot represent this. Which atom-type, torsion-ownership and proton-construction changes must accompany the charge model?

14. **Junction parameter scope:** Can renamed junctions carry explicit chemical roles, including the partner frame, rather than borrowing rows through atom names alone? How should both ends being renamed, carbonyl oxygen versus hydroxyl, N-substitution, retained hydrogens, and non-peptide polymers be covered? The new `0593a93b0` helper maps nucleotide phosphate to peptide nitrogen; single-frame substitution also leaves the remote atom names canonical. A connection-specific parameter contract would make the supported scope explicit.

15. **Mirror sampling contract:** Should paired L/D structures receive one-to-one reflected conformer sets, including terminal defaults, probability truncation and extra-chi sampling? Which equivalent-atom permutations are allowed, and what grid-boundary convention makes the probability-ordering tables agree with their reflected counterparts? Matching scores and preserving D chirality do not establish this.

16. **Achiral glycine model:** Is `with_symmetric_gly()` intended to symmetrize the complete model or only select backbone tables? The two alpha-hydrogen ideal lengths and bonded targets differ, which breaks exact reflection after rebuilding equivalent hydrogens. Also, a reflection-invariant omega potential can retain backbone dependence; what supports replacing it with a uniformly trans table? The follow-up preserves the default parameter files and makes the hydrogen averaging opt-in.

17. **Private library grids:** Are custom backbone origins, anisotropic spacing and smaller periodic tables supported? Can that contract be checked through both scoring and sampling, including serialized reflected libraries and mixtures of differently sized tables? These cases expose inherited layout assumptions that default 36×36 grids conceal.

</details>


Additional input-contract questions from the expanded corpus: how should a caller declare authoritative versus incomplete connectivity, and where should provenance for inferred edges live? How should mutually crosslinked polymer ports be resolved before generating patches, and which layer rejects invalid combined torsion frames? Findings 114–117 give concrete triggers, implemented fixes and remaining limits.
